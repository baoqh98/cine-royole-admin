import { Container, Space, Title } from '@mantine/core';
import React from 'react';

type Props = {};

const Showtime404 = (props: Props) => {
  return (
    <Container>
      <Space h={160} />
      <Title>Bạn cần chọn phim để tạo được lịch chiếu</Title>
    </Container>
  );
};

export default Showtime404;
